{
  "name": "velocity-typing-tutor",
  "version": "0.1.0",
  "description": "Velocity Arcade — a six-game typing tutor that runs in the browser with no dependencies",
  "license": "MIT",
  "scripts": {
    "serve": "npx serve web-app -p 3000 --no-clipboard",
    "test": "playwright test",
    "test:e2e": "playwright test tests/e2e",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "serve": "^14.2.3"
  }
}
